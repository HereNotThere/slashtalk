---
"@slashtalk/electron": minor
---

Inactive teammates (idle >24h) are now hidden from the rail by default and surface as a hover-expanding stack only when the new "Show inactive teammates" tray toggle is on. Replaces the previous "Stack inactive teammates" toggle — stacking is now the only display mode for inactive peers.
